---
id: "diary-2026-03-30"
type: memory
tags: ["api-routes", "performance", "n-plus-1", "docker"]
summary: "Completed core API routes. Fixed N+1 query (800ms→45ms). Started Dockerfile."
refs:
  - "know-pg-tuning"
  - "know-docker"
---

# 2026-03-30 Work Log

Completed core API routes:
- POST /api/auth/login — JWT login
- GET /api/products — product list with pagination
- POST /api/orders — create order
- GET /api/orders/:id — order detail

Introduced Zod for request validation, wrote unified error handling middleware.

Afternoon: fixed an N+1 query problem. Orders list endpoint was querying product info individually per order. Switched to JOIN, response time dropped from 800ms to 45ms. @attr(performance_gain: "17.7x", technique: "JOIN-replace-N+1")

Evening: started writing Dockerfile with multi-stage build to reduce image size.
