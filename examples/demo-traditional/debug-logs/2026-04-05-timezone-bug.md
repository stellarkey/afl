# 2026-04-05 时区 Bug 修复

## 现象
用户反馈订单创建时间显示不正确。美国用户看到的时间比实际早了 8 小时。

## Root Cause
服务器在 UTC+8 时区，`new Date()` 返回的是本地时间，但存入数据库时没有做时区转换。

## 修复
1. 数据库字段改为 `TIMESTAMP WITH TIME ZONE`
2. 服务端统一用 `new Date().toISOString()` 存储 UTC 时间
3. 前端根据用户 locale 做显示转换

## 影响
已有数据中约 200 条订单的创建时间差了 8 小时，写了迁移脚本批量修正。
