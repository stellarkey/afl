# 2026-03-30 工作日志

完成了核心 API 路由：
- POST /api/auth/login — JWT 登录
- GET /api/products — 产品列表，支持分页
- POST /api/orders — 创建订单
- GET /api/orders/:id — 订单详情

引入了 Zod 做请求校验，写了统一的错误处理中间件。

下午处理了一个 N+1 查询问题。orders 列表接口会为每个订单单独查 product 信息，改用 JOIN 后响应时间从 800ms 降到了 45ms。

晚上开始写 Dockerfile，用了多阶段构建来减小镜像体积。
